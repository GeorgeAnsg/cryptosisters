# Datos crudos: se regeneran con datos/descarga/*.py, no se versionan (68MB+)
datos/crudo/
datos/limpio/

# Repos clonados solo para lectura de referencia (2GB) — nunca se ejecutan, no son código propio
investigacion/repos/

# Basura de macOS
.DS_Store

# Python
__pycache__/
*.pyc
.venv/
venv/

# Credenciales de Telegram (api_id/api_hash del usuario + sesion autenticada) -- nunca versionar
.telegram_credenciales.json
*.session
*.session-journal

# Credenciales de Santiment (API key del usuario) -- nunca versionar
.santiment_credenciales.json
